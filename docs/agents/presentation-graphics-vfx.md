# Agent · Graphics & VFX  ·  Presentation pillar

**Owns.** Scene render, post-processing stack, shaders, particles, CRT overlay, NES palette, PS1/affine texture look.

**Reports into** → [Presentation pillar](../pillars/presentation.md) · also reads [Shared Core](../SHARED_CORE.md)

## Grounded in (external canon)
- [Particle system](https://doc.babylonjs.com/features/featuresDeepDive/particles/particle_system/particles)
- [Post-processes](https://doc.babylonjs.com/features/featuresDeepDive/postProcesses/usePostProcesses)
- [Materials / shaders](https://doc.babylonjs.com/features/featuresDeepDive/materials)

## Internal docs
- `docs/ART_DIRECTION.md` _(author + maintain)_

## Invariants
- Visual-only — particles, post, and shaders never feed back into sim state.

## Working log
_Append-only. Newest at top. Each entry: date · decision/change · open issues._

- 2026-06-30 · PS1/FF9 rendering + perf: four tasks, presentation-only, no js/sim.js touch, no ?v= bump. (1) Shadow generator removed: replaced `new BABYLON.ShadowGenerator(2048, sunLight)` and all config/refresh-rate lines with a stub `{ addShadowCaster: () => {} }` so the eight `shadowGenerator.addShadowCaster(m)` calls in model-loader callbacks become silent no-ops; `window.shadowGenerator = null` keeps the two `if (window.shadowGenerator)` castle guards correct; the separate `castleShadowGen` (1024, castleLight) for castle self-shadows is untouched. Removed the shadow-frustum bounds block (orthoLeft/Right/Top/Bottom, autoCalcShadowZBounds) that was only needed to define the shadow-map projection. (2) Character blob shadows: already implemented in the file as the `buildBlobShadows` IIFE (~line 7580) — four `CreateGround` blobs (blobShadow0–3, width=1.7, height=1.15, y=0.09, renderingGroupId=0, receiveShadows=false, zOffset=-3, alpha=0.5, radial-gradient DynamicTexture) whose XZ is updated per-frame from the four paddle positions; matches the working projectile blob pattern exactly. Updated stale `alpha` comment from "sun shadow is the primary cast shadow" to clarify blob is now the only shadow. (3) SSAO2 pipeline removed: deleted the `new BABYLON.SSAO2RenderingPipeline("ssao", ...)` block and all `.radius/.totalStrength/.base/.samples/.maxZ/.minZAspect/.expensiveBlur` config lines; no other code referenced `ssaoPipeline`. Posterize post-process and glow layer are unaffected. (4) Foliage instanced: created 3 hidden source IcoSpheres (`bushSrc0/1/2`, radius=1, subdivisions=1, `isVisible=false`) before `makeBush`; refactored `makeBush` to call `bushSources[matIdx].createInstance("bushInst")` instead of `CreateIcoSphere` per blob; instance scaling set to `(r, r*0.7, r)` matching the original radius-r mesh + `scaling.y=0.7` squash; material variety preserved via random `matIdx`; wind-wobble UV animation on the shared `leafTex` propagates automatically to all instances; renderingGroupId stays default 0 (group 0, behind characters at group 1). node --check passed. · Open: castle shadow generator still runs every frame (1024² shadow map) — could also be replaced with blob/decal if desired, but it was not in scope; `receiveShadows=true` on arena floor/walls/bases is now a harmless no-op (no shadow map for the sun light) but left in place to minimise diff.

- 2026-06-30 · Three graphics/presentation tasks (no sim touch, no js/sim.js edits). (1) Render-resolution slider (50–100%, step 5, default 100%): `applyRetroResolution()` now reads `Settings.values.renderScale`; HSL formula is `setHardwareScalingLevel(1 / (dpr * (pct/100)))` — at 100% equals the prior `1/dpr` (no visual change at default). Exposed as `window.applyRetroResolution`; persisted in Settings; wired into wireSettingsScreen/openSettings. (2) FPS-cap dropdown (Uncapped/120/90/60/30, default Uncapped): added `renderFpsCap`/`lastRenderCapTime` locals + `window.setRenderFpsCap` setter; the `else if (renderFpsCap > 0)` branch in `window.gameRenderLoop` gates only `scene.render()` — the `scene.onBeforeRenderObservable` accumulator uses `engine.getDeltaTime()` (delta from previous render) so at e.g. 30 FPS cap it batches 2 × 16.67 ms sim steps per frame; sim timing is unchanged. PS1 throttle path is untouched. Persisted in Settings. (3) Bush occlusion: assigned `renderingGroupId = 1` to all loaded character meshes in all five loaders (loadPickleWizard active; loadMeshyWizard/loadCharacterWithAnims, loadCharacterModels P1+P2, loadDoublesCharacters P3+P4 for future modes). Scheme: bushes stay group 0; characters move to group 1 (same as fog plane + cast bars + juice flames); projectiles stay group 2. Within group 1, BabylonJS depth-sorts opaque characters first then blends the transparent fog plane over them — identical to the previous group-0-under-fog arrangement, so the reactive fog look is preserved. Syntax-checked with node --check. · Open: `settings-select` has no CSS style defined yet (falls back to browser native); cosmetic only. Shadow casters on characters still render at group 0 (shadow maps are depth-only, unaffected by renderingGroupId). - 2026-06-29 · Re-drove SP-only presentation after Step 2.3-C routed singles through the deterministic sim: added `updateSinglePlayerPresentation(dt)` (index.html ~12026) called from `runSinglePlayerFrame` after `simulateNetworkFrame`. Rewired: cast bars (2-D + 3-D + show/hide, ~12032-12056), paddle targetRotY + moveDir from `lastMoveDir` (~12062-12085), player footstep SFX with boundary guard (~12087-12108), ice-block freeze visuals + shatter-on-expire (~12110-12133), cooldown + mana UI at stepped rate (~12135-12140), per-projectile trail anchor + blob shadow + fireball/frostbolt flicker/glow + loop-sound pan/pitch + approach SFX (~12142-12209). FX-only confirmed: zero writes to proj.x/z/velX/velZ, paddleZ, mana, cooldowns, casting, castProgress, freezeTime. Syntax-checked with node --check. · Open: `proj.collisionGraceTime` is never decremented in the new SP path (the sim reads it for proj-vs-proj collision gating but does not tick it — a pre-existing sim gap that affects split-projectile self-collision); fix belongs in js/sim.js when that path is exercised. Frostbolt trail in PvP also undriven (pre-existing gap, separate scope).
- 2026-06-28 · Fixed PvP match camera showing old menu angle: `resetCameraToDefault`/`flipCameraForClient` set GAME_CAMERA alpha/beta/radius but didn't stop the in-flight menu/talent camera animation (started by showTalentScreen, ~1s smooth tween toward MENU_CAMERA / target (-10,1.5,0)), which kept driving the camera every frame after the snap. Added `cam.getScene().stopAnimation(cam)`+`stopAnimation(cam.target)` to both helpers (mirrors single-player's doReadyGoTransition). Visual-only, no sim feedback; single-player path untouched. · Open: orchestrator to verify live window.gameCamera values in PvP vs SP match; opponent-name exchange still TODO.
- _(start here)_

---
_[Presentation pillar](../pillars/presentation.md) · [Shared Core](../SHARED_CORE.md) · [Master](../../PROJECT.md)_
