# Agent · Graphics & VFX  ·  Presentation pillar

**Owns.** Scene render, post-processing stack, shaders, particles, CRT overlay, NES palette, PS1/affine texture look.

**Reports into** → [Presentation pillar](../pillars/presentation.md) · also reads [Shared Core](../SHARED_CORE.md)

## Grounded in (external canon)
- [Particle system](https://doc.babylonjs.com/features/featuresDeepDive/particles/particle_system/particles)
- [Post-processes](https://doc.babylonjs.com/features/featuresDeepDive/postProcesses/usePostProcesses)
- [Materials / shaders](https://doc.babylonjs.com/features/featuresDeepDive/materials)

## Internal docs
- `docs/ART_DIRECTION.md` _(author + maintain)_

## Invariants
- Visual-only — particles, post, and shaders never feed back into sim state.

## Working log
_Append-only. Newest at top. Each entry: date · decision/change · open issues._

- 2026-07-06 · Task 3 (charge orb): added `_odOrbMakeSphere` helper + `orbKey` to `_BEAM_SIDE_DEFS` (hoisted, no per-frame string alloc); `BC.orbWhite` Color3(1,1,1) in `_overdriveBeamColors` (separate from BC.core, never mutated); per-side lazy-create of orbRoot TransformNode + white inner sphere (d=0.5) + team-color outer sphere (d=0.8) + 4 crackle thin boxes (0.8×0.04×0.04), all ALPHA_ADD rg=1; per-frame update inside V2 IIFE loop before windup `continue`: swell 0.3→1.6 with accelerating sin pulse during windup, snaps to 1.1 as muzzle core during beam; crackle re-randomized every ~4 frames via per-orb `_crackleFrame` counter; Reduce Motion: pulse=0 + crackle disabled (swell kept as gameplay telegraph). Zero steady-state allocations. Syntax OK; HTTP 200. · Open: Tasks 4 (endpoint FX) still pending; no two-peer MP test yet.

- 2026-07-06 · Review fix (beam alloc): hoisted `_BEAM_SIDE_DEFS`, `_BEAM_TR_FALLBACK`, `_odBeamMakeMat`, `_odBeamMakeBox` to module scope before `updateMatchPresentation`; removed per-frame `sideDefs` array + two object literals + two function declarations from the IIFE; added explicit `root.position.y = 0` in per-frame update. Zero steady-state allocations confirmed. Syntax OK; HTTP 200. · Open: none new.

- 2026-07-06 · Replaced v1 Overdrive beam (single stretched box) with three-row Kamehameha body in `updateMatchPresentation` (index.html). Per side: `_overdriveBeamRoot_{left,right}` TransformNode + three child boxes (core 0.35×0.5, mid 0.5×1.2, glow 0.65×1.8=2×BLOCK_TOL); emissive+ALPHA_ADD materials; white core, team-color mid, deep team-color glow. Per-frame: endpoint derived from read-only block test (|casterZ−oppZ|≤BLOCK_TOL → blocked→opp paddle X; connecting→goalX=±14); eruption wipe 0→full over 0.15s anchored at muzzle; end-thin last 0.3s (core/mid Z-heights→0, glow stays); ramp feedback on core Z-height ×(1+0.5·rampFrac); ±2% jitter on core length only (skipped under `_reduceMotion`). Zero per-frame allocations: six Color3s cached in `_overdriveBeamColors`, right-side mid/glow updated via `.set()` each frame for colorblind-swap responsiveness. Exposed `window._overdriveBeamRoot_{left,right}` for Tasks 3-4. Gate-X = 14 (matches sim `const goalX = 14` in js/sim.js). Syntax check clean; server 200. · Open: Tasks 3 (charge orb) and 4 (endpoint FX) position against these roots; no two-peer MP test yet.

- 2026-07-05 · Fixed PvP per-frame FX (trails, ice/freeze, rotation lean, footsteps, cast bars, mana UI): renamed `updateSinglePlayerPresentation` to `updateMatchPresentation`; added call from `runPvPGameLoop` after the fixed-step sim while-loop (settled state, isResimulating=false); removed now-redundant `updatePvPCastBars()` call and its mana UI duplicates from the PvP loop; deleted the stale `updatePvPCastBars` function (had wrong camera-flip-indexed cast-bar routing — no flip in PvP). updateMatchPresentation is fully read-only w.r.t. sim state; determinism oracles unaffected. node script parse OK. · Open: MP FX correctness requires two live peers to confirm; proj.approachSoundPlayed is a presentation flag that was already present in SP — no new state concerns.

- 2026-07-03 · Painted forest backdrop (Andrew's art, Higgsfield) added as textures/backdrop_forest.jpg behind the whole arena. Renders as a Babylon background Layer inside the 3D pass (PS1 grade applies; HUD stays on top). Self-aligning: alignBackdrop() runs per frame AFTER the screen-shake observable with force-fresh camera matrices — projects the walls (z=±halfDepth) to screen and solves the layer texture's vScale/vOffset so the painted rails (rows 294/492 of 768, constants BACKDROP_*_RAIL_T) land exactly on the real walls; uScale preserves art aspect, centered; vScale is NEGATED (layer V runs opposite image rows — a positive scale renders the art upside down, verified by pixel correlation). Handles resize, letterboxing, render scale, and the PvP camera flip. Towers: all end-structure meshes parented to playerTowerRoot/aiTowerRoot, offset via setTowerOffset(0.4, -1.0) so the base slabs sit flush against the stage band (no backdrop slivers). Screen shake now moves the backdrop in lockstep (full-frame shake) and is globally damped to 55% (SHAKE_SCALE). · Open: Andrew wants to fine-tune offsets/art later; new backdrop iterations only need the two RAIL_T constants updated.
- 2026-06-30 · Block barrier forcefields (presentation-only, no js/sim.js touch, no ?v= bump): added `buildBlockBarriers` IIFE (~line 7748 in index.html) immediately after the blob-shadow IIFE. Two `BABYLON.MeshBuilder.CreatePlane` meshes (`playerBlockBarrier` / `aiBlockBarrier`), each BARRIER_W=3.8 (Z-axis) × BARRIER_H=1.8 (Y-axis). Player plane: rotation.y=+π/2 (normal→+X toward field), position.x=paddleX+0.5; AI plane: rotation.y=-π/2 (normal→-X), position.x=paddleX-0.5; both position.y=0.9 (center, spans y=0…1.8). Per-mesh 32×32 DynamicTexture pixel-art pattern: clearRect, dim team-color fill (alpha 0.22), 1px grid lines every 8px (0.88 opacity), 4×4 diamond rune accents at cell centers (0.72 opacity), linear-gradient top+bottom edge highlight (0.55 opacity); `imageSmoothingEnabled=false` + `updateSamplingMode(NEAREST_SAMPLINGMODE)` for crisp retro pixels. Material: `disableLighting=true`, `emissiveColor=teamColor*0.55`, `diffuseTexture=tex`, `useAlphaFromDiffuseTexture=true`, `alpha=0.40`, `backFaceCulling=false`, `fogEnabled=false`, `renderingGroupId=2` (above fog plane group 1). Team colors: blue (0.43,0.72,1.0) for player, red (1.0,0.42,0.33) for AI. `scene.onBeforeRenderObservable` per-frame: alpha pulse (0.35+0.08*sin(t*0.0022)), vOffset UV scroll (t*0.000055 mod 1), Z tracking from paddle.position.z. FX-only confirmed: zero writes to sim state, no js/sim.js edits, no ?v= bump. node --check passed. · Open: BARRIER_W (3.8), BARRIER_H (1.8), alpha range (0.35–0.43), x-offset (±0.5), UV scroll rate are all starter values — lead tunes in-browser. backFaceCulling=false means the barrier is visible from behind the wizard too (acceptable for readability; lead may want true if it looks odd from that angle).

- 2026-06-30 · Tower/castle brightness reduction + PS1 baked grunge variation (presentation-only, no js/sim.js touch, no ?v= bump). (1) Stone brightness: wherever stone mats set diffuseColor=(1,1,1), changed to Color3(0.62,0.62,0.62) (~0.62× multiplier) — applies to playerGateMat, aiGateMat, keystoneMat (applied in stoneImg.onload), keepMat and parapetMat (set immediately in createGate plus registered for async grunge swap). (2) Team-colored surfaces darkened ~0.65×: blueBaseMat (0.3,0.5,1.0)→(0.20,0.33,0.65); redBaseMat (1.0,0.4,0.35)→(0.65,0.26,0.23); playerBackdropMat same blue scale; aiBackdropMat same red scale. (3) Roof: diffuseColor=teamCol.scale(0.60) (was full teamCol); emissiveColor=teamCol.scale(0.08) (was 0.18) — darker/richer, still clearly team-colored. (4) Keystone: MCM.goldBright.scale(0.82) — keeps gold identity, slightly toned. (5) Grunge compositing: replaced loadGateTextures BABYLON.Texture approach with new Image() + DynamicTexture pipeline. stoneImg.onload creates buildGrungeTex(name,uS,vS): draws tower_stone.png onto 512×512 DynamicTexture canvas, overlays 30 dark elliptic radial-gradient splotches (alpha 0.06–0.15, rx 18–70, ry 12–46) + 9 light splotches (alpha 0.04–0.09) using seeded PRNG (seed 9137, LCG). Called once per unique tiling: wallDT(1,1) for gate+merlons; buttressDT(1,3) for buttresses (independent grunge pass for variety); keep(2,5) and parapet(3,1) via _pendingKeepMats deferred list populated by createGate. All static — zero per-frame work. Seeded PRNG is decorative-only, no sim state read/write. node --check passed. · Open: _pendingKeepMats grunge DTs have different patterns than the gate because the RNG state has advanced through 4 buildGrungeTex calls — this is intentional variation; lead may want to tune alphas in-browser.

- 2026-06-30 · Baked PS1/FF9 painted ground shadows for static scenery (towers + bushes): (1) Added `getStaticShadowMaterial()` lazy getter (~line 762) — one shared `StandardMaterial` using a 64×64 DynamicTexture radial gradient (dark centre → transparent edge), `disableLighting=true`, `useAlphaFromDiffuseTexture=true`, `fogEnabled=false`, `alpha=0.35`, `zOffset=-3`; same proven-visible recipe as the existing character/projectile blob shadows. (2) Task A — Tower shadows: an IIFE after `window.redBase` builds two `CreateGround` planes (`blueTowerShadow` / `redTowerShadow`) at y=0.08, renderingGroupId=2; each 8 units wide (baseWidth×2) × ~14 units deep (baseDepth×0.88); left tower centre shifted +1.8x (rightward/toward field), right tower centre shifted -1.8x (leftward/toward field); both biased +0.4z per sun direction. (3) Task B — Bush shadows: a hidden source `CreateGround` plane (`bushShadowSrc`, 1×1, isVisible=false, renderingGroupId=2) created inside `buildFoliage` before `makeBush`; inside `makeBush` one `createInstance("bushShadowInst")` per cluster at (x, 0.08, z) scaled to `(scale*1.8, 1, scale*1.5)` — one shadow per cluster, not per blob. No per-frame work: all static, no onBeforeRender usage for these meshes. Presentation-only: no js/sim.js touch, no ?v= bump, no sim state reads or writes. node --check passed. · Open: tower shadow alpha=0.35 and offset/size are starter values; lead tunes visually in-browser. Bush shadow instances inherit source renderingGroupId=2 correctly in BabylonJS.

- 2026-06-30 · PS1/FF9 rendering + perf: four tasks, presentation-only, no js/sim.js touch, no ?v= bump. (1) Shadow generator removed: replaced `new BABYLON.ShadowGenerator(2048, sunLight)` and all config/refresh-rate lines with a stub `{ addShadowCaster: () => {} }` so the eight `shadowGenerator.addShadowCaster(m)` calls in model-loader callbacks become silent no-ops; `window.shadowGenerator = null` keeps the two `if (window.shadowGenerator)` castle guards correct; the separate `castleShadowGen` (1024, castleLight) for castle self-shadows is untouched. Removed the shadow-frustum bounds block (orthoLeft/Right/Top/Bottom, autoCalcShadowZBounds) that was only needed to define the shadow-map projection. (2) Character blob shadows: already implemented in the file as the `buildBlobShadows` IIFE (~line 7580) — four `CreateGround` blobs (blobShadow0–3, width=1.7, height=1.15, y=0.09, renderingGroupId=0, receiveShadows=false, zOffset=-3, alpha=0.5, radial-gradient DynamicTexture) whose XZ is updated per-frame from the four paddle positions; matches the working projectile blob pattern exactly. Updated stale `alpha` comment from "sun shadow is the primary cast shadow" to clarify blob is now the only shadow. (3) SSAO2 pipeline removed: deleted the `new BABYLON.SSAO2RenderingPipeline("ssao", ...)` block and all `.radius/.totalStrength/.base/.samples/.maxZ/.minZAspect/.expensiveBlur` config lines; no other code referenced `ssaoPipeline`. Posterize post-process and glow layer are unaffected. (4) Foliage instanced: created 3 hidden source IcoSpheres (`bushSrc0/1/2`, radius=1, subdivisions=1, `isVisible=false`) before `makeBush`; refactored `makeBush` to call `bushSources[matIdx].createInstance("bushInst")` instead of `CreateIcoSphere` per blob; instance scaling set to `(r, r*0.7, r)` matching the original radius-r mesh + `scaling.y=0.7` squash; material variety preserved via random `matIdx`; wind-wobble UV animation on the shared `leafTex` propagates automatically to all instances; renderingGroupId stays default 0 (group 0, behind characters at group 1). node --check passed. · Open: castle shadow generator still runs every frame (1024² shadow map) — could also be replaced with blob/decal if desired, but it was not in scope; `receiveShadows=true` on arena floor/walls/bases is now a harmless no-op (no shadow map for the sun light) but left in place to minimise diff.

- 2026-06-30 · Three graphics/presentation tasks (no sim touch, no js/sim.js edits). (1) Render-resolution slider (50–100%, step 5, default 100%): `applyRetroResolution()` now reads `Settings.values.renderScale`; HSL formula is `setHardwareScalingLevel(1 / (dpr * (pct/100)))` — at 100% equals the prior `1/dpr` (no visual change at default). Exposed as `window.applyRetroResolution`; persisted in Settings; wired into wireSettingsScreen/openSettings. (2) FPS-cap dropdown (Uncapped/120/90/60/30, default Uncapped): added `renderFpsCap`/`lastRenderCapTime` locals + `window.setRenderFpsCap` setter; the `else if (renderFpsCap > 0)` branch in `window.gameRenderLoop` gates only `scene.render()` — the `scene.onBeforeRenderObservable` accumulator uses `engine.getDeltaTime()` (delta from previous render) so at e.g. 30 FPS cap it batches 2 × 16.67 ms sim steps per frame; sim timing is unchanged. PS1 throttle path is untouched. Persisted in Settings. (3) Bush occlusion: assigned `renderingGroupId = 1` to all loaded character meshes in all five loaders (loadPickleWizard active; loadMeshyWizard/loadCharacterWithAnims, loadCharacterModels P1+P2, loadDoublesCharacters P3+P4 for future modes). Scheme: bushes stay group 0; characters move to group 1 (same as fog plane + cast bars + juice flames); projectiles stay group 2. Within group 1, BabylonJS depth-sorts opaque characters first then blends the transparent fog plane over them — identical to the previous group-0-under-fog arrangement, so the reactive fog look is preserved. Syntax-checked with node --check. · Open: `settings-select` has no CSS style defined yet (falls back to browser native); cosmetic only. Shadow casters on characters still render at group 0 (shadow maps are depth-only, unaffected by renderingGroupId). - 2026-06-29 · Re-drove SP-only presentation after Step 2.3-C routed singles through the deterministic sim: added `updateSinglePlayerPresentation(dt)` (index.html ~12026) called from `runSinglePlayerFrame` after `simulateNetworkFrame`. Rewired: cast bars (2-D + 3-D + show/hide, ~12032-12056), paddle targetRotY + moveDir from `lastMoveDir` (~12062-12085), player footstep SFX with boundary guard (~12087-12108), ice-block freeze visuals + shatter-on-expire (~12110-12133), cooldown + mana UI at stepped rate (~12135-12140), per-projectile trail anchor + blob shadow + fireball/frostbolt flicker/glow + loop-sound pan/pitch + approach SFX (~12142-12209). FX-only confirmed: zero writes to proj.x/z/velX/velZ, paddleZ, mana, cooldowns, casting, castProgress, freezeTime. Syntax-checked with node --check. · Open: `proj.collisionGraceTime` is never decremented in the new SP path (the sim reads it for proj-vs-proj collision gating but does not tick it — a pre-existing sim gap that affects split-projectile self-collision); fix belongs in js/sim.js when that path is exercised. Frostbolt trail in PvP also undriven (pre-existing gap, separate scope).
- 2026-06-28 · Fixed PvP match camera showing old menu angle: `resetCameraToDefault`/`flipCameraForClient` set GAME_CAMERA alpha/beta/radius but didn't stop the in-flight menu/talent camera animation (started by showTalentScreen, ~1s smooth tween toward MENU_CAMERA / target (-10,1.5,0)), which kept driving the camera every frame after the snap. Added `cam.getScene().stopAnimation(cam)`+`stopAnimation(cam.target)` to both helpers (mirrors single-player's doReadyGoTransition). Visual-only, no sim feedback; single-player path untouched. · Open: orchestrator to verify live window.gameCamera values in PvP vs SP match; opponent-name exchange still TODO.
- _(start here)_

---
_[Presentation pillar](../pillars/presentation.md) · [Shared Core](../SHARED_CORE.md) · [Master](../../PROJECT.md)_
