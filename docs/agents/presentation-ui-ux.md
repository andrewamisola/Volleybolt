# Agent · UI / UX  ·  Presentation pillar

**Owns.** FF9 command menu (Babylon GUI), DOM menus, ability bar, combat log, party stats, the FF9 gem-panel restyle.

**Reports into** → [Presentation pillar](../pillars/presentation.md) · also reads [Shared Core](../SHARED_CORE.md)

## Grounded in (external canon)
- [Babylon GUI](https://doc.babylonjs.com/features/featuresDeepDive/gui/gui)
- [NES.css](https://nostalgic-css.github.io/NES.css/)

## Internal docs
- `docs/UI.md` _(author + maintain)_

## Invariants
- Follow the Babylon-GUI-vs-DOM decision rule recorded in UI.md.

## Working log
_Append-only. Newest at top. Each entry: date · decision/change · open issues._

- 2026-07-05 · Fix MP cast-bar inversion: rewrote CAST BARS section of `updateMatchPresentation` (~lines 12705–12788) to derive `localC`/`remoteC` from `gameMode`+`isHost` instead of hard-mapping by left/right; 2-D bar visibility (showCastBar/showAICastBar) and fill (playerCastFill.width / aiCastFill.width) now both track the same combatant — fixes joiner inversion and fill-not-moving; 3-D bars remain positional (leftC/rightC); SP routing unchanged; report at `.superpowers/sdd/castbar-fix-report.md`. · Open: PvP correctness needs 2-peer live test; `startCasting` one-shot path still uses legacy `caster==='player'` check (SP-only path, not triggered from PvP sim).

- 2026-07-05 · Play Again button on victory/defeat screen: renamed existing `#victoryContinueBtn` from "Continue" to "Main Menu" (behavior unchanged); added new `#victoryPlayAgainBtn` DOM button (same `.game-btn menu-btn` class, positioned at `calc(540px + 92px)` — 48 px above Main Menu at `540px + 140px`) shown/grace-disabled in `showVictoryScreen`, hidden in `hideVictoryScreen`; SP click path: `_startRematchSP()` clears VFX, hides victory, calls `resetGame()`, resets `gameState="menu"` + `startGameInProgress=false` then `startGame()` — `aiSkillRating` (localStorage ELO) untouched; MP click path: `_returnToConnectedLobby()` sends `{type:'REMATCH'}` to peer, clears VFX, hides victory, sets `GameSM.state='lobby'`/`show3D(false)`, shows `lobbyConnectedView`, calls `resetReadyState()` — PeerJS connection kept alive; `handleNetworkMessage` gains `case 'REMATCH':` that runs `_returnToConnectedLobby()` on the peer side; both buttons are real focusable `.game-btn` elements so gamepad d-pad nav between them works without code changes. Report: `.superpowers/sdd/play-again-report.md`. · Open: MP rematch not headlessly verifiable (needs 2 peers); human live test required.

- 2026-07-02 · Follow-up pass: (1) messageText ("Midfield Lost!", "Champion", stage names) and victoryTitle switched to Ferrum, matching the transition cards; hidden #htmlMessage mirror kept in sync in styles.css. (2) How to Play rebuilt as a 4-page carousel — one concept per page (Pong with spells / Advance or fall back / Mana is momentum / Controls), .htp-title in Ferrum, fps-arrow prev/next + gold dot pager, ArrowLeft/Right keys navigate with a cursor blip, page resets to 1 on open; controller-hint IDs (htpControllerHeader, hintAbilities, hintUlt, hintParry) preserved for updateControllerHints. (3) Clean beauty shots captured to screenshots/ (no HUD, no fps-monitor, native 2880x1620). · Open: none.
- 2026-07-02 · Four HUD/transition changes: (1) Debug menu (backtick) gained a Hide HUD toggle — body.hud-hidden CSS hides #hudScaleLayer, and window.enforceHudHidden() re-hides the Babylon GUI HUD controls every frame (event-driven code re-shows them on round/stage changes, so a one-shot hide wouldn't stick); the command/stats observable checks window.hudHidden itself. (2) transitionText (Ready…/Go! + stage-name cards) switched FF9UI→52px Ferrum — the FF title font. (3) Stage transitions no longer show floating text: doStageTransition now calls hideMessage() after resetRound (the stage-name center message used to linger over the reveal), clearAllCombatText() sweeps DOM combat text/damage numbers/cast bubbles at transition start (DOM layer renders above the canvas, so leftovers floated over the black overlay), and createHTMLCombatText/showDamageNumber early-return while roundTransitioning. (4) returnToMenuTransition also clears combat text. · Open: netcode perf overlay (F2) intentionally NOT hidden by Hide HUD — it's a debug tool itself.
- 2026-07-01 · Damage numbers + spell-cast announcements migrated from DOM to Babylon GUI so they pixelate with the scene at any hardware scaling level. `showDamageNumber`: no longer creates a `div.damage-number`; instead builds a transparent `BABYLON.GUI.Rectangle` + `TextBlock` (48 px FF9UI, team-colored: red `#ff6060` = player tower hit, blue `#4bb8ff` = AI tower hit) added to `window.guiTexture`. `updateDamageText`: now steps via `STEP_INTERVAL` (40 ms) and drives the rect's `top` and `alpha` using piecewise linear interpolation matching the old CSS `damageFloat` keyframes (pop-up 0–0.2 s, bounce 0.2–0.35 s, float 0.35–0.86 s, fade 0.86–1.1 s); disposes the rect at 1.1 s. `showSpellCastText`: no longer calls `createHTMLCombatText`; instead creates a `Rectangle` (160×36 px, `rgba(40,34,28,0.92)` fill, 2 px team-colored border, `cornerRadius=4`, FF9UI 18 px text) parked at −9999 px until the first update tick; pushed to `spellCastQueue` with `{ guiRect, age }`. `updateSpellCastText`: X scale changed from 1920/renderWidth to 1440/renderWidth (Babylon GUI idealWidth); position now sets `guiRect.left/top` to center the Rectangle at the computed anchor; fade sets `guiRect.alpha`; expiry calls `guiRect.dispose()`. The `damageTextQueue` and `spellCastQueue` item shape changed (`.el` → `.guiRect`); no other queues touched. `floatingTexts`, `combatTextQueue`, and `ailmentText` still use DOM `.el`. CSS `.cast-bubble`, `.damage-number`, `@keyframes damageFloat` remain in `styles.css` but are no longer written to (dead CSS, kept for reference). · Open: CSS rules above can be removed in a future cleanup pass; spell-cast box width (160 px) may clip very long ability names — increase `CAST_W`/`CAST_HALF_W` if needed.

- 2026-06-30 · Settings screen game-feel pass: (1) FPS Cap — replaced `<select id="setFpsCap">` with a `<div class="fps-cycler">` containing `<button id="setFpsCapPrev">` / `<span id="setFpsCapVal">` / `<button id="setFpsCapNext">`; wired cycler in wireSettingsScreen (FPS_OPTIONS=[0,120,90,60,30], calls Settings.set('fpsCap', val) with same values as before); openSettings restore code reads Settings.values.fpsCap to set label on open. (2) Range sliders — expanded `.settings-slider` in styles.css from single accent-color line to full `-webkit-appearance:none` custom track (dark steel, border, inset shadow) + gold gem thumb (`::webkit-slider-thumb`, `::moz-range-thumb`); applies to all four sliders (Master/Music/SFX/RenderRes) uniformly. (3) Fullscreen checkbox — replaced two-line rule with styled pill toggle (`::after` circle sliding on :checked, gold when on). (4) FPS cycler styling — added `.fps-cycler` / `.fps-arrow` / `.fps-val` classes (steel background matching game-btn grain, border hover to gold, 3px drop shadow, FF9UI font). node --check passes. No sim/render/Settings API touched. · Open: none.

- 2026-06-30 · Spell-cast floating text: anchored above 3D cast bar + non-overlapping queue. Decoupled spellCastQueue {player,ai} from combatTextQueue; new window.updateSpellCastText registered in render loop (line 13863). Anchor: projects cast-bar top edge (paddle world Y + 2.09 world units) to HUD screen space; bubble centre placed BUBBLE_HALF_H(17px) + ABOVE_GAP(8px) above that projected point. Stack: slot 0 = newest (bottom), each older entry offset -48px (STACK_SPACING, exceeds ~41px bubble+tail height); 1 s hold then 25fps stepped upward drift; stepped fade in last 0.5 s; queue capped at 4. showSpellCastText rewired to push to spellCastQueue instead of calling showCombatText; FX-only, no sim state touched; node --check passes. · Open: doubles-mode front combatants share the same player/ai side key as back combatants; if a front and back combatant cast simultaneously their bubbles share one queue (benign stacking). If per-combatant queues are needed, plumb combatant identity through showSpellCastText and add four queue slots.

- 2026-06-29 · Landing page revision D (4 changes): (1) Single CTA — removed "Join the Playtest" and "Wishlist" buttons; one "Join Discord" / "Join Discord to Playtest" button with href="#" placeholder in both hero and CTA section. (2) Hook — primary line changed to "Pong, Evolved."; two alternatives noted in an HTML comment (B: "Pong, Enchanted.", C: "Pong with Spells."); sub-line unchanged. (3) Seamless hero — added <video autoplay loop muted playsinline> with TODO source comment; hero uses ::before for gradient overlay and ::after for scanlines; hero-bottom-fade element dissolves bottom edge into narrative with linear-gradient; removed border-top from #narrative. (4) Faithful tooltips — replaced .vb-tooltip/.vb-tt-* with exact game DOM structure (.ability-tooltip, .tooltip-title, .tooltip-stats-row, .tooltip-stats-left, .tooltip-stat-item.{mana|cast|cooldown}, .tooltip-description, .tt-*); CSS hex values resolved directly (no CSS vars): #f0c050 gold, #c9b896 parchment, #3060b0 blue-light (mana), #c03030 red-light (cooldown), #1a0f0a brown-dark (arrow), full tt-* accent palette; tooltip positions right on desktop / above on mobile with matching arrows. Note: game's .ability-slot DOM bar is visibility:hidden at runtime; faithful reproduction is of the DOM markup pattern, not the visible Babylon GUI spell-detail box (which uses steel + #5ad0ff mana). · Open: video src + poster + og:image/og:url still placeholder; Discord invite URL still placeholder; owner should confirm hook line choice from alternatives.
- 2026-06-29 · Created landing page at docs/landing/index.html: single-page promotional site using vb-* design system components; ability tooltips pulled verbatim from index.html; all three CTA links are href="#" placeholders; no invented facts, counts, quotes, or dates; mobile-responsive. Hook chosen: "Pong, but you're a wizard." · Open: owner needs to fill in Join the Playtest URL, Discord invite URL, and Wishlist URL; og:image/og:url meta tags marked as PLACEHOLDER.
- 2026-06-29 · Audit pass on design-system files: removed invented parchment/stone sub-palette (5 tokens); restyled .vb-tooltip to the real steel panel treatment (STEEL/RIM constants); replaced all brown-tone hex values with verified game tokens; renamed "arcane" labels to neutral descriptions across all three files; grep confirms zero parchment/brown/arcane/brown-hex references remain; all retained hex values verified present in index.html/styles.css. No game files changed. · Open: see prior entry.
- 2026-06-29 · Created design-system extraction under docs/design-system/: STYLE-GUIDE.md (full token catalog + component specs), volleybolt-ui.css (standalone CSS with :root tokens + component classes), demo.html (static showcase of all components). No game files changed. · Open: local custom fonts (FF9UI.ttf, Ferrum.otf) are not web-hosted — website build will need to either bundle them or accept Press Start 2P / Cinzel fallbacks; the CSS already declares both. The CRT overlay is authored and documented but currently disabled in-game; the demo shows it on-by-default for marketing purposes.

- _(start here)_

---
_[Presentation pillar](../pillars/presentation.md) · [Shared Core](../SHARED_CORE.md) · [Master](../../PROJECT.md)_
